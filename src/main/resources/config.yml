# ChatItemSprite Configuration

# Enable or disable the entire plugin feature
enabled: true

# Enable or disable LuckPerms formatting support
luckperms-formatting: false

# chat format for LuckPerms. Ignored if luckperms-formatting is false
# ONLY supports MiniMessage formatting. Do not use legacy color codes (&, §)`
chat-format: "{prefix} {player}: {message}"

# Item display format
# Available modes:
#   text-only - Display text only (e.g., [Netherite Sword x1])
#   sprite-only - Display sprite only (e.g., [<sprite:items:item/netherite_sword>])
#   both - Display both sprite and text (e.g., [<sprite:items:item/netherite_sword> Netherite Sword x1])
item:
  display-mode: "both"

  # Format for unsupported items (like player_head, shield, tipped_arrow, etc.)
  #   text - Display text (e.g., [Player Head x1])
  #   none - Display nothing (e.g., [])
  unsupported-item-fallback: "text"
  
  # Format for text display (used in 'text-only' and 'both' modes)
  # Available placeholders:
  #   {name} - Item display name (displaying name by player's language if Vanilla)
  #   {amount} - Item quantity
  text-format: "{name} x{amount}"
  
  # Message when player is holding nothing (air)
  empty-hand-format: "[Empty Hand]"
  
  # Show hover tooltip with full item info
  show-hover-info: true
  
  # Wrap display with brackets (applies to all modes)
  wrap-in-brackets: true

# Replace all [item] tags or just the first one
replace-all: true

# Messages (supports MiniMessage formatting)
# MiniMessage examples: <green>, <red>, <bold>, <gold>, <italic>, <#FF5555>, etc.
# ONLY supports MiniMessage formatting. Do not use legacy color codes (&, §)`
messages:
  reload-success: "<green>Configuration reloaded!"
  no-permission: "<red>You don't have permission to reload this plugin."
  plugin-version: "<gray>ChatItemSprite v{version}"
  usage: "<gray>Usage: /chatitemsprite reload"
  unknown-command: "<red>Unknown subcommand. Usage: /chatitemsprite reload"